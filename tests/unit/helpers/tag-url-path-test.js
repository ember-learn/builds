import { tagUrlPath } from 'builds/helpers/tag-url-path';
import { module, test } from 'qunit';

module('Unit | Helper | tag url path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagUrlPath([42]);
  assert.ok(result);
});
