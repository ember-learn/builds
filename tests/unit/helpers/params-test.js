import { params } from 'builds/helpers/params';
import { module, test } from 'qunit';

module('Unit | Helper | params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = params([42]);
  assert.ok(result);
});
